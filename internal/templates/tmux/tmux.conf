# Agency session configuration — loaded at server startup via tmux -f.

# Hide the status bar; agency's sidebar is the source of truth.
set-option -g status off

# Enable mouse support for pane selection and resizing.
set-option -g mouse on

# Clear all default key bindings, then add the only one agency needs:
#   C-Space — toggle between sidebar and workspace pane
unbind-key -a
bind-key -n C-Space last-pane
